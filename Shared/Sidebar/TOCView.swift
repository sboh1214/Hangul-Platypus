import SwiftUI

struct TOCView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct TOCView_Previews: PreviewProvider {
    static var previews: some View {
        TOCView()
    }
}
