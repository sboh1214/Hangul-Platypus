import SwiftUI

struct TOCView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct TOCViewPreviews: PreviewProvider {
    static var previews: some View {
        TOCView()
    }
}
